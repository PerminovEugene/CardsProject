!!!5
%html
  %head
    %meta{:content => "main", :name => "layout"}/
    %title Ввод текста
  %body
    / Declare Variable
    - text = '  Введите текст поздравления (максимум 650 символов)'
    - sign = ' Подпишитесь, оставьте контактные данные (максимум 250 символов'
    .nav
      .container
        %ul
          %li
            .point
              .circle.active
                1
            .line.active
          %li
            .point
              .circle.active
                2
            .line.active
          %li
            .point
              .border
                .circle.active
                  3
            .line
            .point.last
              .circle
                4
    .mail-style
    .container.page
      %h1 Заполните обратную сторону
      %g:form.js-form{:controller => "card", :action => "save", :enctype => "multipart/form-data"}
        .card
          %g:textArea.text{:name => "card_text", :value => "${session.currentCard.text}", :placeholder => "$text"}/
          %input.file{:type => "file", :id => "logo",  :name => "logo", :value => "${session._logo}"}/
          %g:textArea.sign{:name => "card_sign", :value => "${session.currentCard.sign}", :placeholder => "$sign"}/
        .footer
          %g:link.button.button-prev{:controller =>"envelope", :action=>"index"} На шаг назад
          %a.button.button-next.js-submit Далее
          %span.error-text.js-error